---
- name: self-apply bootstrap configuration
  hosts: localhost
  connection: local
  gather_facts: true
  become: true
  vars_files:
    - vars/main.yml

  roles:
    - role: base
    - role: tools
    - role: ai
    - role: security