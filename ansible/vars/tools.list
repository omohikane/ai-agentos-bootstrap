# ansible/vars/tools.list
# General packages, one per line. '#' starts a comment.
# Installed with pacman. Prefix 'aur:' for AUR-only packages (installed via yay).
# Editing this file is one of the user's main tasks.

git
ripgrep
tmux
neovim
jq
htop
docker